// №68 Создайте массив с числами. Выведите его на экран с помощью функции alert, а также выведите его в консоль с помощью console.log.
let arr = [1, 2, 3];
console.log(arr);
alert(arr);

// Создайте массив со строками. Выведите его на экран с помощью функции alert, а также выведите его в консоль с помощью console.log.
let arr = ['a', 'b', 'c'];
console.log(arr);
alert(arr);

// №69 Создайте массив с элементами 1, 2, 3. Выведите на экран каждый из этих элементов.
let arr = [1, 2, 3];
console.log(arr[0]);
console.log(arr[1]);
console.log(arr[2]);

// Дан следующий массив: let arr = [1, 2, 3] Выведите на экран сумму элементов этого массива.
let arr = [1, 2, 3];
let sum = arr[0] + arr[1] + arr[2];
console.log(sum);

// let arr = ['a', 'b', 'c', 'd']; Выведите с помощью этого массива следующую строку: 'a+b+c+d'
let arr = ['a', 'b', 'c', 'd'];
let result = arr[0] + '+' + arr[1] + '+' + arr[2] + '+' + arr[3];
console.log(result);

// №70 Создайте массив с произвольными элементами. Выведите на экран количество элементов в этом массиве.
let arr = ['apple', 'banana', 'orange', 'grape'];
console.log(arr.length);

// Создайте массив с произвольными элементами. Выведите на экран последний элемент этого массива.
let arr = ['apple', 'banana', 'orange', 'grape'];
console.log(arr[arr.length - 1]);

// №71 Создайте массив с элементами 'a', 'b', 'c'. Запишите вместо первого элемента число 1, вместо второго - 2, вместо третьего - 3.
let arr = ['a', 'b', 'c'];
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;
console.log(arr);

// №72 Создайте массив с числами. Прибавьте к каждому элементу массива число 3. Выведите на экран измененный массив.
let numbers = [1, 2, 3, 4, 5];
numbers[0] += 3;
numbers[1] += 3;
numbers[2] += 3;
numbers[3] += 3;
numbers[4] += 3;
console.log(numbers);

// №73 Создайте массив с элементами 1, 2 и 3. С помощью оператора ++ увеличьте каждый элемент массива на единицу.
let arr = [1, 2, 3];
arr[0]++;
arr[1]++;
arr[2]++;

// №74 С помощью описанного приема создайте массив с элементами 1, 2 и 3.
let arr = [];
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;

// Пусть дан такой массив: let arr = [1, 2, 3];Добавьте ему в конец элементы 4 и 5.
let arr = [1, 2, 3];
arr[arr.length] = 4;
arr[arr.length] = 5;

// №75 let arr = []; arr[3] = 'a'; arr[8] = 'b';
let arr = [];
arr[3] = 'a';
arr[8] = 'b';
console.log(arr.length);

// №76 Заполните массив числами 1, 2 и 3.
let arr = [];
arr.push(1, 2, 3);

// Пусть дан такой массив: let arr = [1, 2, 3];Добавьте ему в конец элементы 4 и 5.
let arr = [1, 2, 3];
arr.push(4, 5);

// №77 Дан следующий массив: let arr = ['a', 'b', 'c']; Дана также переменная: let key = 2; Выведите на экран элемент, ключ которого хранится в переменной key.
let arr = ['a', 'b', 'c'];
let key = 2;
console.log(arr[key]);

// Дан следующий массив: let arr = [1, 2, 3, 4, 5];Даны также переменные: let key1 = 1; let key2 = 2;Найдите сумму элементов, ключи которых хранятся в наших переменных.
let arr = [1, 2, 3, 4, 5];
let key1 = 1;
let key2 = 2;
let sum = arr[key1] + arr[key2];

// №78 Дан массив: let arr = ['a', 'b', 'c', 'd', 'e']; Удалите из него два элемента.Проверьте, какое станет значение свойства length после этого.
let arr = ['a', 'b', 'c', 'd', 'e'];
delete arr[1];
delete arr[2];
console.log(arr.length);

// №79 В следующих задачах некоторый программист написал код и, возможно, допустил в нем ошибки. Вы должны проверить, делает ли код то, что описано. Если код работает некорректно, вы должны исправить ошибки. Код должен вывести последний элемент массива: let arr = [1, 2, 3, 4, 5]; console.log(arr[arr.length]);
let arr = [1, 2, 3, 4, 5];
console.log(arr[arr.length - 1]);

// Код должен найти сумму элементов массива: let arr = [1, 2, 3, 4, 5]; console.log(arr[1] + arr[2] + arr[3] + arr[4] + arr[5]); 
let arr = [1, 2, 3, 4, 5];
console.log(arr[0] + arr[1] + arr[2] + arr[3] + arr[4]);

// Код должен вывести длину массива: let arr = { 1, 2, 3, 4, 5}; console.log(arr.length]);
let arr = [1, 2, 3, 4, 5];
console.log(arr.length);

// Код должен вывести длину массива: let arr = [1, 2, 3, 4, 5]; console.log(arr.lenght);
let arr = [1, 2, 3, 4, 5];
console.log(arr.length);


















